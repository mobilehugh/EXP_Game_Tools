Find what: \([^ ]*\) \(.*\) Replace with: \U\1\E \2

the \U will cause all following chars to be upper

the \E will turn off the \U




       # print out size wate age
        self.set_xy(8, self.get_y())
        self.set_font("Helvetica", size=10)
        line_height = self.font_size * 1.4
        blob = f"**Persona Details:** {self.object.Persona_Name} is {self.object.Age} {self.object.Alien_Age_Suffix} old.  {self.object.Size} sized, weighing {self.object.Wate} {self.object.Wate_Suffix}."
        line_width = 0  # full width
        self.cell(
            w=line_width,
            h=line_height,
            markdown=True,
            txt=blob,
            ln=1,
            fill=False,
            border=False,
        )